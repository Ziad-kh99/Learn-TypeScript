

const usr = {
    name: 'Ziad', 
    age: 23
};

user.location;

const msg = 'some words';

msg();

const announcement1 = 'Hello World';

announcement.toLocaleLowercase();
announcement.toLocalLowerCase();
announcement.toLocaleLowerCase();


function FlipCoin() {
    return Math.random < 0.5;
}

const value = Math.random() < 0.5 ? 'a' : 'b';
if(value !== 'a') {
    //..
} 
else if(value === 'b') {
    // Ops, unreachable..
}